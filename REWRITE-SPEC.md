# 六份配置重写需求方案

> 编写日期：2026-09-25。用途：下一次交给 AI 执行完整重写。
> 核心要求：**全新安装、初始化状态的 Clash Party，仅开启 TUN，导入订阅链接并添加／绑定覆写即可使用。DNS 覆写、嗅探覆写及其余应用选项全部保持初始化状态。**
> 用户已彻底重装 Clash Party，目前仅开启 TUN 并下载了订阅；本方案以此为唯一客户端使用与验收基准。
> 本次只编写需求方案；配置重写、客户端切换和发布在后续执行。

## 1. 重写目标与范围

重新编写四份 Clash Party YAML 覆写和两份 iOS Shadowrocket 配置，使结构清楚、注释准确、同场景一致、后续容易维护。重写必须保留当前功能覆盖，不能用大幅删减规则的简化版替代现有配置。

优先级：**稳定可用与导入即用 > 分流正确与兼容 > 跨文件一致 > 结构简洁 > 新功能与优化**。

本轮允许整理章节、统一格式、消除有证据的重复、补齐遗漏说明、完善必要校验。保持当前服务分类、代理／直连意图和有效节点下的出口偏好。为实现导入即用，允许对 DNS 合并兼容、配置自包含及初始节点选择作必要调整；逐项记录调整理由与验证结果，不把额外设置步骤转交给用户。

### 1.0 首要交付条件：无需额外设置

使用基准明确为：**全新安装／初始化完成 → 仅开启 TUN → 导入订阅链接与对应覆写并绑定 → 直接使用**。导入订阅和覆写的先后顺序不限。用户当前已经完成重装、开启 TUN 和导入订阅，后续只需添加并绑定覆写，无须重新初始化或重复导入订阅。首次安装客户端需要的系统授权、有效订阅和可用节点属于基本运行条件。

- “DNS 覆写”“嗅探覆写”“DNS 策略”等选项均保持所安装版本的初始化值，不要求用户打开或关闭，也不通过脚本、内部配置写入或其他方式代替用户修改这些选项。
- 不要求用户手填 DNS、编辑客户端内部配置、运行修复命令、安装辅助脚本，或更改系统／浏览器网络设置才能正常访问。
- 开启 TUN 是唯一明确允许的应用设置操作，用户已完成；TUN 协议栈、网卡探测、路由等其余选项保持初始化值，不增加后续调节步骤。
- 首次导入、更新订阅、更新覆写和重启后均应可继续使用，不依赖某次临时确认、隐藏状态或旧缓存。
- 默认策略应自动形成可用代理路径；手动切换节点／地区只是可选偏好操作，不能用于补救配置初始缺陷。
- README 的正常使用流程只写初始化客户端、开启 TUN、导入订阅与覆写并绑定，不附额外设置清单。开发者的验证细节放在验证记录中。

**“改完几个开关就能用”不算完成本需求。** 若目标客户端限制使纯 YAML 无法满足某项要求，交付应明确标为未满足并给出具体证据，不宣称已实现，也不擅自用更多用户操作替代要求。

### 1.1 六份主交付文件

| 文件 | 使用场景 | 使用方式 | 默认分流 |
| --- | --- | --- | --- |
| `rule_single.yaml` | 内地，单地区或少量节点 | Clash Party 订阅覆写 | 国内直连，海外代理 |
| `rule_multi.yaml` | 内地，多地区订阅 | Clash Party 订阅覆写 | 国内直连，海外代理 |
| `rule_special.yaml` | 港澳，单地区或少量节点 | Clash Party 订阅覆写 | 默认直连，指定服务代理 |
| `rule_special_multi.yaml` | 港澳，多地区订阅 | Clash Party 订阅覆写 | 默认直连，指定服务代理 |
| `shadowrocket.conf` | 内地，iOS | Shadowrocket 配置 | 国内直连，海外代理 |
| `shadowrocket_special.conf` | 港澳，iOS | Shadowrocket 配置 | 默认直连，指定服务代理 |

文件名与既有 raw 地址保持不变，不恢复旧的 `rule_single_special.yaml` / `rule_multi_special.yaml` 入口。“单地区”不等于只能有一个节点，必须支持日常节点与家宽节点同时存在。

### 1.2 配套交付

- 保留并按需要同步 `rules/shadowrocket/` 中现有七份 `.list`，避免六个主文件改好了、引用的规则仍旧过期。
- 更新 `README.md`，准确说明六份文件的选择、导入绑定、默认策略、验证与恢复方法；移除正常使用流程中的额外开关要求。
- 按需要完善已有校验脚本；交付实际运行过的验证记录和相对基线的行为差异表。
- 不提交订阅节点、密码、令牌、客户端完整配置或含私密连接信息的日志。

不以新模板系统、新构建工具、JavaScript 覆写或客户端源码修改为交付前提。六份主文件必须可以按现在的方式直接使用。

## 2. 基线必须认对

### 2.1 仓库基线

本方案编写时实际检查到：

- 当前分支：`main`。
- 当前提交：`c5420b9b14ebcaa693d7d752b00a914e0627319f`。
- 六份主配置与该提交一致，无未提交的内容差异。
- `2886e3a` 位于 `backup/pre-rollback-20260925`，属于回滚前备份，**不是本轮重写基线**。
- `.audit/`、旧需求草案和备份分支包含不同阶段的试验及结论，只作排查线索，不能直接恢复其中的修改。

执行时先重新核对 `git status` 与 `git rev-parse HEAD`。若用户已在此基础上作了新修改，保留用户修改并记录差异，不擅自 reset、checkout 覆盖或恢复备份分支。

### 2.2 唯一客户端基准：重装后的初始化状态，仅开启 TUN

以下状态由用户最新说明确定，本轮没有重新探测或修改客户端：

| 项目 | 使用与验收要求 |
| --- | --- |
| 安装状态 | Clash Party 已彻底重装，保持初始化状态 |
| TUN 开关 | 已开启，保留开启状态 |
| 订阅 | 已通过链接下载；只需添加并绑定对应覆写 |
| DNS 覆写 | 保持初始化值，不手动或自动改开关 |
| 嗅探覆写 | 保持初始化值，不手动或自动改开关 |
| 其他应用／TUN 高级选项 | 全部保持初始化值 |
| 旧配置、旧缓存、旧确认记录 | 不作为可用性的依赖，不恢复重装前环境 |

“初始化值”指本次实际安装版本的默认值，不在本方案里用重装前观察到的布尔值或 DNS 池替代。执行重写时只读记录版本及初始值，用于验证；不得先修改应用再称其为初始状态。

**仓库基线用于保留服务分流，客户端基线用于验证导入即用，两者分开。** 本方案此前记录的重装前应用设置、手工 DNS 池与运行时快照已失效，不再作为兼容目标或恢复目标。客户端不需要同时适配多套人为开关组合。

README 中要求关闭 DNS 覆写、调整嗅探等旧说明必须删除。配置应在上述唯一基准下工作，不能要求用户再次调试刚重装的客户端。

### 2.3 本轮已经核实的范围

`ruby scripts/validate.rb` 已通过：四文件结构、同场景 DNS／规则一致性、地区正则反例、默认路由、直连保护顺序和港澳多地区 AI 默认项。

上述检查是此前对仓库配置的静态验证，不代表重装后环境已经验收。本轮仅更新需求文档，未切换配置、未重启客户端、未进行 iOS 实机测试，也未重新验证全部远程规则、节点连通或平台解锁。

## 3. 导入即用的使用方式

### 3.1 Clash Party

用户流程如下；已完成的步骤不需要重复：

1. 使用初始化状态的 Clash Party，开启 TUN，其他应用选项不动。
2. 导入订阅链接，并在“覆写”中添加对应版本的文件或链接。
3. 将订阅绑定到这份覆写后直接使用，不再进入设置页调整 DNS、嗅探或其他选项。

配置与实现要求：

- 订阅提供节点，覆写提供 DNS、规则、策略组等；四份 YAML 不是独立订阅，不写入真实节点或机场信息。
- 一个订阅只绑定四份中的一份；开发者验证全局覆写和订阅覆写的实际作用范围，不将排查冲突作为正常安装步骤。
- 更新订阅仍能保留本项目的规则与策略组，客户端最终结果不能重复追加同名组或旧路由。
- 保留规则分流、策略选择记忆和 fake-ip 缓存意图；除已明确开启的 TUN 外，不要求用户再启用其他功能。
- 保留 `dns!`、`tun!`、`sniffer!`、`rule-providers!`、`geox-url!` 的整对象替换意图；`rules` 与 `proxy-groups` 保持当前替换方式，不改成不断追加。
- 不通过修改节点协议、节点凭据、监听端口或控制接口来“修复”规则问题。

Clash Party 的 YAML 覆写优先级低于应用常用配置；`!` 控制覆写时的对象替换，不会锁住后续应用设置。实现必须在这一机制下验证导入即用，不能以要求用户关闭应用覆写来绕过兼容问题。[官方覆写说明](https://clashparty.org/docs/guide/override/yaml)

实现时按实际安装版本只读核对合并顺序与 DNS 守卫，验证“默认应用设置 + TUN 开启 + 订阅 + 覆写”的最终结果。不依赖守卫、脚本或其他机制将应用选项改成非初始状态才获得可用性，也不恢复重装前的设置进行测试。

### 3.2 Shadowrocket

1. 先导入节点或订阅，再导入并启用对应地区的 `.conf`；全局路由使用“配置”。
2. 应用组选 `PROXY` 时沿用首页当前节点；`ALL` 用于手动选择，`AUTO` 用于测速选择。
3. 保留应用策略组，不能退化成只有 `DIRECT` / `PROXY` 的简版规则。
4. 不移植 Clash 专用字段、MRS 文件或 YAML 正则写法到 Shadowrocket。
5. 保持无脚本、无重写、无生效 MITM 主机列表，不新增证书安装要求。

节点／配置选择与正常启用属于客户端的基本操作；不得另加手动 DNS、解析兼容或网络参数设置流程。

## 4. 分流需求

下表描述默认选择下的目标行为。用户保存过的策略选择可能覆盖“首选项”，验收须同时记录策略组和最终出口。

| 业务 | 内地 YAML | 港澳 YAML | 内地 Shadowrocket | 港澳 Shadowrocket |
| --- | --- | --- | --- | --- |
| 私网、局域网 | 全球直连 | 全球直连 | DIRECT | DIRECT |
| 国内支付、银行、政务、中国 AI、腾讯及游戏保护清单 | 全球直连，先于广告 | 同左 | DIRECT，先于广告 | 同左 |
| 广告及现有遥测清单 | 广告组，默认 REJECT | 同左 | REJECT | REJECT |
| OpenAI / ChatGPT、Anthropic / Claude | AI 平台 | AI 解锁 | AI | AI |
| 现有 Google AI 开发工具清单 | AI 平台 | AI 解锁 | AI | AI |
| Gemini 网页入口 | AI 平台 | 保持直连 | AI | 保持直连 |
| OpenRouter、Grok、Perplexity 等补充 AI | AI 平台 | AI 备选，默认 DIRECT | AI | AI_EXTRA，默认 DIRECT |
| Google 普通业务、GitHub | 节点选择 | 全球直连 | PROXY | DIRECT |
| Telegram、YouTube | 各自应用组 | 全球直连 | 各自应用组 | DIRECT |
| Netflix、Disney+ 等现有主流流媒体 | 流媒体解锁 | 保持默认直连 | Streaming | 保持默认直连 |
| Hulu 及港澳指定流媒体清单 | 流媒体解锁 | 流媒体解锁 | Streaming | Streaming |
| Microsoft / Apple 普通业务 | 各自应用组，默认 DIRECT | 全球直连 | 同名组，默认 DIRECT | DIRECT |
| 国际学术平台补充清单 | 节点选择，优先于国内兜底 | 全球直连 | PROXY，优先于国内兜底 | DIRECT |
| 未匹配流量 | 漏网之鱼，默认节点选择 | 全球直连 | PROXY | DIRECT |

具体边界必须保留：

- 港澳 Google AI 开发工具清单目前为 `aistudio.google.com`、`generativelanguage.googleapis.com`、`ai.google.dev`、`notebooklm.google.com`、`notebooklm.google`、`labs.google`、`antigravity.google`、`workspace.google.com`。保持已有匹配粒度，不扩大到全部 Google 域名。
- 港澳额外流媒体清单为 `peacocktv.com`、`paramountplus.com`、`cbsaavideo.com`、`pluto.tv`、`tubi.tv`、`bbc.co.uk`、`bbci.co.uk`，另保留 Hulu 上游规则。
- 港澳 `bbc.co.uk` / `bbci.co.uk` 使用流媒体策略；`bbc.com` 保持直连。不恢复 `iplayer` 关键词，不承诺按网页路径区分 BBC 内容。
- 港澳 TikTok 保持当前默认直连；不因为“海外服务”统一改成代理。
- 港澳 `custom_proxy_domain` 继续保留当前两个域名，沿用 AI 解锁组；iOS 对应内联规则仍指向 AI。当前两个后缀为 `search.agonygao.top`、`luckyg.131518.xyz`。
- 当前内地 `ai_static` 包含 `DOMAIN-SUFFIX,bing.com`，因此 Bing 本体也会进入 AI 组。这是现状，不写成已经修复；缩小范围属于单独的行为调整。
- 表中的服务类别以实际规则集为边界；上游可能更新，不把未核实的新入口或新品牌域名写成既有覆盖。
- “家宽”“美国”“AI 解锁”是节点筛选／策略名称，不是 IP 属性认证或平台可用性保证。

## 5. YAML 策略组要求

下列组名、数量和默认顺序描述基线。有效的家宽／地区候选存在时保持当前偏好；缺少偏好候选时，按第 5.5 节自动选择可用代理路径。允许为此增加必要的辅助组，但必须无循环、无默认直连回落，并在差异表中说明。不能为了维持旧组数而留下首次导入就不可用的默认路径。

### 5.1 内地单地区：`rule_single.yaml`

保留当前 11 个组及默认方向：

| 组名 | 默认项／要求 |
| --- | --- |
| `🚀 节点选择` | 筛选后的实际节点；空组 REJECT；无默认 DIRECT 入口 |
| `🏠 家宽` | 按节点名称筛选；空组 REJECT |
| `🤖 AI 平台` | `🏠 家宽` → `🚀 节点选择` → `DIRECT` |
| `📲 电报消息`、`📹 油管视频`、`🎬 流媒体解锁` | `🚀 节点选择` → `DIRECT` |
| `Ⓜ️ 微软服务`、`🍎 苹果服务`、`🎯 全球直连` | `DIRECT` → `🚀 节点选择` |
| `🛑 广告拦截` | `REJECT` → `DIRECT` |
| `🐟 漏网之鱼` | `🚀 节点选择` → `DIRECT` |

此处箭头表示基线选项顺序，本身不提供自动故障切换。实现需补足缺少家宽候选时的初始化回退，不能要求用户先进入 AI 组手动改选，也不能静默换成直连。

### 5.2 内地多地区：`rule_multi.yaml`

保留上述应用组和家宽默认项，增加当前地区结构，合计 23 个组：

- 香港、台湾为地区手动选择组。
- 日本、新加坡、美国各保留地区入口、自动组、手动组。
- 保留全局 `♻️ 自动选择`；总入口首选全局自动，并可选择地区、实际节点及现有显式 DIRECT 选项。
- `include-all` 同时兼容订阅直接列出的节点与 proxy-provider 节点。
- 地区组只向本地区自动／手动组及实际节点引用；不得回指总入口或互相形成循环。
- 地区自动组只测速本地区节点。手动组引用本地区自动组的现有结构可以保留。
- 保留现有测速地址、300 秒间隔、容差与超时；不把测速成功解释为平台解锁成功。

### 5.3 港澳单地区：`rule_special.yaml`

保留当前 7 个组：节点选择、家宽、AI 解锁、AI 备选、流媒体解锁、广告拦截、全球直连。

- `🤖 AI 解锁` 默认家宽，随后节点选择、DIRECT。
- `🧪 AI 备选` 默认 DIRECT，随后家宽、节点选择。
- `🎬 流媒体解锁` 默认节点选择。
- `🎯 全球直连` 默认 DIRECT，并作为最终兜底。

### 5.4 港澳多地区：`rule_special_multi.yaml`

保留当前 19 个组及地区结构：

- `🤖 AI 解锁` 首选 `🇺🇸 美国`，保留其他地区、家宽及总入口选项。不能改成按全局最低延迟选 AI 出口。
- `🧪 AI 备选` 首选 DIRECT，保留 AI 解锁与总入口选项。
- `🎬 流媒体解锁` 首选节点选择，可独立选择地区与家宽。
- 有美国候选时保持美国优先；没有美国候选时按第 5.5 节自动回退到可用代理节点，不以空美国组阻断默认 AI 路径。仍不将全局最低延迟等同于 AI 解锁能力。

### 5.5 节点筛选与空组

保留当前家宽和信息节点过滤规则。地区识别支持中英文地名、旗帜与有字母边界的缩写，不只凭运营商名字判地区。

最低测试样本：`HK 01`、`TPE 01`、`JP 01`、`SG 01`、`US 01`、`US Residential`、`Australia 01`、`CMI 美国 02`、`Premium1 美国`、`VPS`、`家宽`、`剩余流量 100G`。

要求：Australia 不误入美国组；后两类运营商样本不误入香港或新加坡；无地区名称的正常节点仍能从总入口选到；信息节点被排除。测试名字仅为合成样本，不硬编码进正式代理组。

空叶子组和没有任何有效节点的全局自动组保留 `empty-fallback: REJECT`；但应用组的初始路径不能因为某个偏好叶子组为空就一直落到 REJECT。至少有可用代理候选时，自动形成“偏好候选优先、缺少时使用通用代理候选”的路径，不要求用户改组或给节点重命名。

自动回退只保证有可转发的代理路径，不承诺任意节点都能解锁服务。某些父组存在显式 DIRECT 选项可以保留，但不能将其改成自动兜底。订阅本身无有效节点或节点全部不可达时可明确失败，不伪装成规则已正常可用。

回退机制必须用目标内核支持的行为实现和验证，不能把 `select` 的选项顺序、空组 fallback 或一次测速误当作已经实现跨组自动切换。

## 6. DNS、TUN 与嗅探：配置自行兼容

### 6.1 DNS

- 四份 YAML 保留完整、自洽的 `dns!`，不能删除解析器字段后依赖应用偶然补齐。
- 只以“初始化设置 + TUN 已开启”为目标；DNS 覆写及 DNS 策略选项保持初始化值，基本解析、节点启动、路由与访问结果必须满足业务要求。不要求适配用户主动切换后的其他开关组合。
- 订阅有 DNS、没有 DNS、含自定义 DNS 策略时都要验证，避免依赖订阅提供偶然缺失的字段或客户端某次确认状态。
- 保留 fake-ip 模式、地址池、黑名单过滤方式、缓存参数和现有兼容性排除项；不新增全匹配 `*`。
- 保持本地回环监听 `127.0.0.1:1053`，不扩大为局域网 DNS 服务。
- 保留 `respect-rules: true` 和有效的 `proxy-server-nameserver`，节点域名解析不能陷入依赖该节点自身的循环。
- 内地两份保持当前国内默认／直连／节点解析池以及海外规则对应的 Cloudflare、Google DoH 策略。
- 港澳两份保持当前公共 DoH 默认／节点解析池、国内域名策略及指定海外服务策略，不新增 `direct-nameserver`。
- 同场景两份的 DNS 字段、策略引用及顺序完全一致；所有 `rule-set:` 名称都能在本文件找到。
- 默认沿用现有 DNS 端点，不顺便增加新的解析实验。若实现导入即用确需调整字段、合并方式或解析路径，可作最小必要修改并提供回归证据，不把“去设置里改 DNS”作为解决方法。

`respect-rules` 处理 DNS 连接的路由，不保证每个业务查询都使用该业务相同的出口；引导解析、节点解析、业务解析应分开验证。`direct-nameserver-follow-policy` 在官方文档中有定义，不能凭历史报告的字符串检索就认定无效并删除，应结合目标内核确认。[mihomo DNS 文档](https://wiki.metacubex.one/config/dns/)

### 6.2 TUN 与嗅探

- TUN 保持开启；应用中的协议栈、自动路由、接口探测与 DNS 劫持等选项保持初始化值。覆写中的相关字段须与此兼容，验证最终合成结果，不要求用户手动填成旧配置的数值。
- 不硬编码出口网卡名、设备编号或机器专属路由；不将自动探测改成关闭。
- 保留现有 HTTP／TLS／QUIC 嗅探配置及米家、Apple 推送兼容项，不增加大范围嗅探排除。
- 嗅探覆写保持初始化值，在该状态下验证分流；不切换嗅探开关寻求通过，不增加多套开关组合的兼容要求。
- 不修改操作系统 DNS、网络服务顺序、防火墙或客户端安装文件来完成规则重写。
- 在已开启 TUN 的初始化环境中完成核心业务分流，不关闭 TUN 或改协议栈来补救导入失败。本轮不以 TUN 关闭状态作为交付测试场景。
- 应用设置是开发者必须处理的合并输入，不是交给用户逐项配置的前置条件。README 直接给出导入流程，技术差异写入开发者验证说明。

TUN 的路由和劫持支持范围与系统有关，验收以目标客户端的实际结果为准，不把字段存在当成全系统接管证明。[mihomo TUN 文档](https://wiki.metacubex.one/config/inbound/tun/)

## 7. 规则集与顺序

按当前规则逐条建立迁移清单，再重写；同场景单地区、多地区只能在代理组结构上有区别。不得为减少行数删除私网、腾讯游戏、支付银行、学术平台、Apple TV 或海外 AI 补充等已有覆盖。

内地按现有顺序组织：

1. 私网与系统连通性检查。
2. 既有 DoH／解析检测站代理规则及防泄漏补充集。
3. 国内敏感业务、中国 AI、腾讯及游戏直连保护。
4. 广告与遥测。
5. AI、Telegram、YouTube。
6. 现有流媒体规则。
7. GitHub、国际学术、Google 及其国内例外。
8. Microsoft、Apple；AI 与 Apple TV 等专项规则已经优先。
9. Steam 国区、国内游戏、中国域名和 IP 兜底。
10. 非中国域名规则及 `MATCH,🐟 漏网之鱼`。

港澳按现有顺序组织：

1. 私网。
2. 国内敏感业务、中国 AI、腾讯及游戏直连保护。
3. 广告与遥测。
4. Google AI 开发工具、OpenAI、Anthropic、`chat.com`。
5. 当前自定义代理域名、默认直连的海外 AI 补充组。
6. Hulu 与港澳指定流媒体。
7. `MATCH,🎯 全球直连`。

港澳不得加入内地的“海外全部代理”兜底，也不强行将普通解析检测网站改走代理。

远程规则继续沿用现有源、格式与缓存路径约定，检查 `behavior` / `format` / 引用方式匹配。保留更新周期和必要 Geo 数据，避免把不同文件来自不同域名误写成自动容灾机制。下载成功与规则内容正确是两项独立检查。

## 8. Shadowrocket 两份配置

### 8.1 结构和基础参数

保留 `[General]`、`[Proxy Group]`、`[Rule]`、`[Host]`、空 `[MITM]` 的现有结构。

沿用当前 DNS 主／备用池、`dns-direct-system`、`dns-direct-fallback-proxy`、`private-ip-answer`、`hijack-dns`、`always-real-ip`、`skip-proxy`、`tun-excluded-routes`、`block-quic` 和不支持 UDP 时 REJECT 的设置。不要为了使字段看起来与 YAML 一样而重做 iOS 解析方案。

当前两份 `.conf` 都使用国内 DoH、明文 DNS 与备用 DNS，并允许直连解析失败后经代理兜底；必须准确记录这一行为。港澳 iOS 与港澳 YAML 的 DNS 实现存在差异，不能宣称两端完全等同。

保留目前使用的参数名，不恢复旧参数。涉及 Shadowrocket 特有参数、策略组正则或远程解析选项时，要按目标版本说明与 iOS 导入结果核实；没有设备就标注待实机验证，不从 mihomo 测试推导 iOS 已通过。

### 8.2 策略组与规则

- 内地保留 ALL、AUTO、AI、Telegram、YouTube、Streaming、Microsoft、Apple。
- 港澳保留 ALL、AUTO、AI、AI_EXTRA、Streaming。
- AI 与流媒体默认 PROXY；内地 Microsoft / Apple 默认 DIRECT；港澳 AI_EXTRA 默认 DIRECT。
- 内地必须以 `FINAL,PROXY` 结束；港澳必须以 `FINAL,DIRECT` 结束。不能把两份都改成直连兜底。
- 国内直连保护仍在广告与遥测前；内地 AI／流媒体在宽泛 Microsoft／Apple 规则前，学术清单在国内兜底前。
- 保留当前逐条内联的 `force-remote-dns` 使用方式，不直接搬到 RULE-SET 上。
- 对已有 AdvertisingLite、China、Apple 的 `RULE-SET` 与 `DOMAIN-SET` 成对引用，逐个确认上游实际格式；不能只保留一个地址造成域名覆盖丢失。

### 8.3 七份补充清单的真实对应关系

| 文件 | 对应范围 | 同步要求 |
| --- | --- | --- |
| `direct-supplement.list` | 四个国内保护内联集的并集 | 去重后条目一致 |
| `ai-supplement.list` | 内地 `ai_static` 与 `overseas_ai_extra` 的并集 | 保持等价覆盖；允许消除被同策略父后缀覆盖的子域条目 |
| `overseas-ai-extra.list` | 港澳 `overseas_ai_extra` | 条目一致；不能误用内地较短版本 |
| `streaming-supplement.list` | 内地 `streaming_static` | 条目一致；港澳较小清单保留在 CONF 内联规则中 |
| `telemetry.list` | `telemetry_domain` | 条目一致 |
| `academic.list` | 内地 `academic_platforms` | 条目一致 |
| `proxy-supplement.list` | 内地解析检测／海外 DNS 补充 | 与 YAML 相关远程集及内联规则按覆盖范围核对，不要求伪造逐字相同 |

基线差异说明：内地 `overseas_ai_extra` 为 15 条，港澳为 23 条，这是现有场景差异。`ai-supplement.list` 比 YAML 并集少四条 OpenAI 子域规则，但已经被 `openai.com` 后缀覆盖，不能据此判为漏规则。其他对照集合目前分别为：国内保护去重 124 条、港澳 AI 补充 23 条、内地流媒体补充 16 条、遥测 9 条、学术 89 条。这些数字用于检查意外丢失，不作为永远固定的指标。

本地修改 `.list` 不会自动改变已经写在 CONF 中的远程内容。验证时区分本地文件和线上 raw 文件；发布时六份主配置与七份依赖必须同步。

## 9. 不混入本轮的行为调整

下列项目可记录为后续候选，本次按当前状态保留，不自动实施；第 1.0 节要求的合并兼容和第 5.5 节要求的初始节点回退属于本轮必做适配，不在候选范围内：

- 缩小 Bing 的 AI 规则范围。
- 为 `custom_proxy_domain` 新增独立出口组。
- 新增 Copilot、Sora、新的 Google 入口或其他尚未在基线核实的服务域名。
- 增加遥测封禁范围、扩大学校／校园专用 DNS 规则、替换 DNS 提供商。
- 将港澳额外 AI 改为默认代理，或在美国／家宽候选存在且可用时改变其优先顺序。

每个候选需写清“当前行为、拟改变行为、受影响文件、验证方式”。在用户没有新增要求时，交付应完成现有需求，不因这些候选问题反复停下来询问。

## 10. 验收标准

### 10.1 静态与一致性检查

- 六份文件可解析，无重复键／组名，无悬空策略或规则集引用，无代理组循环。
- 内地两份、港澳两份的基础参数、DNS、TUN、嗅探、规则集和规则顺序分别一致，区别仅在代理组及说明文字。
- 共享且语义相同的内联清单一致；按第 8.3 节保留有意的场景差异，不要求所有同名集合一律相同。
- 国内保护在广告之前，专项服务在宽泛兜底之前，终结规则与第 4 节一致。
- 合成节点、空节点、仅 provider、无家宽、无美国、无地区标记和信息节点等场景都有检查。
- 节点筛选语义要经过目标内核验证，不能只凭其他语言的正则测试判定兼容。
- 七份 `.list` 与配置引用、规则格式及目标策略完整；去重必须证明没有改变首条命中结果。

### 10.2 现有工具的适用边界

| 工具 | 当前状态与执行要求 |
| --- | --- |
| `scripts/validate.rb` | 基础检查本轮已通过；可继续使用，补充必要断言 |
| `scripts/validate.rb --mihomo ...` | 通过构造完整配置做内核检查；不能替代真实客户端合并 |
| `scripts/validate-client.cjs` | 固定了 Clash Party 2.0.2 及旧 bundle 指纹，还依赖特定订阅／旧覆写状态；须按重装后的实际版本及初始化基准适配，不能绕过守卫直接当作有效测试 |
| `scripts/clash-tun-check.sh` | 存在较早的 DNS 管理假设，与本方案不同；未适配前不能作为验收标准 |

本方案只使用当前目录下实际存在、且适配当前版本的工具。不要引用备份分支才有的命令并声称通过，也不为凑齐旧草案的测试命令恢复回滚前脚本。

基础命令：

```sh
ruby scripts/validate.rb
```

内核检查使用当前实际安装的 mihomo，针对**已合成的完整配置**在独立临时工作目录执行 `mihomo -t`。不能直接将含 `dns!` 的覆写文件交给内核，也不能把“简单删掉感叹号”称为完成客户端合并验证。

临时文件如果包含真实节点，只在私有临时目录保存并清理；日志只输出通过／失败及脱敏原因。需要 Python 时，文件中保持 `python` / `python -m ...` 等可移植写法；本机执行遵守工作区环境约定。

### 10.3 客户端合并与真实行为

所有检查共用一个应用设置基准：**全新安装的初始化状态，仅开启 TUN，其余选项不动**。开发者使用隔离配置／测试环境，不操作用户刚重装的应用来凑测试结果：

| 编号 | 测试状态 | 通过条件 |
| --- | --- | --- |
| Z1 | 初始化设置，仅开启 TUN，导入订阅与覆写并绑定 | 四份 YAML 分别完成业务回归，无额外设置步骤 |
| Z2 | 导入前后应用选项对比 | DNS 覆写、嗅探覆写及其他受保护的应用选项仍为初始化值；TUN 始终开启，不能通过隐式修改开关实现可用 |
| Z3 | 无旧规则缓存、DNS 缓存或确认记录的首次加载 | 能完成必要规则下载、节点解析及业务分流，不依赖重装前数据 |
| Z4 | 订阅无 DNS／自带 DNS／自带 DNS 策略 | 节点解析与业务解析自洽；守卫是否介入均不依赖用户额外确认 |
| Z5 | 少量无地区标记节点／无家宽／无美国／仅 provider | 有可用候选时初始策略自动获得代理路径，不要求用户改组或重命名 |
| Z6 | 更新订阅、更新覆写、重启客户端 | 规则和默认路径持续有效，不追加重复内容；DNS／嗅探等选项仍为初始化值，TUN 保持开启 |

1. 记录订阅、覆写、应用设置与合成输出的来源，按当前客户端真实合并流程验证，不以近似合并代替。
2. 四份 YAML 均先在独立环境验证完整配置能被目标内核加载，再检查远程规则下载与业务访问。
3. 分别比较应用选项与最终合成配置：应用选项保持初始化值，合成配置允许按覆写改变 DNS／规则等内容。订阅记录、绑定关系和正常生成的缓存不算额外设置，不能用整个文件逐字不变代替逐项核对。
4. 不再要求“重装前设置也兼容”或“DNS／嗅探开关任意组合都通过”；也不要求适配用户故意设置的全局直连、损坏 DNS 或冲突的第三方覆写。
5. 现场导入前只备份本次重装后的有效文件与订阅绑定；不得将重装前的应用设置备份恢复到当前客户端。
6. 两份 CONF 分别按标准导入／启用流程在 iOS 验证，不另调 DNS 或网络参数；无 iOS 环境时列为待验收。

Z1–Z6 任一必需检查未通过时，“初始化状态导入即用”标为未满足；不附上几个手动修复步骤后改写成通过。目标客户端无法由配置控制的行为，应指出具体合并字段／源码或实测结果及影响，保留需求而不虚报完成。

### 10.4 最小业务回归表

| 样本 | 内地默认预期 | 港澳默认预期 |
| --- | --- | --- |
| `baidu.com`、`bilibili.com`、`alipay.com`、`deepseek.com` | 直连 | 直连 |
| `badjs.weixinbridge.com` | 腾讯直连保护，不被广告优先拦截 | 同左 |
| `chatgpt.com`、`claude.ai` | AI 组 | AI 组 |
| `aistudio.google.com`、`notebooklm.google.com` | AI 组 | AI 组 |
| `gemini.google.com` | AI 组 | 直连 |
| `openrouter.ai`、`grok.com` | AI 组 | AI 备选／AI_EXTRA，默认直连 |
| `google.com`、`github.com` | 普通代理入口 | 直连 |
| `netflix.com` | 流媒体组 | 直连 |
| `hulu.com`、`peacocktv.com` | 流媒体组 | 流媒体组 |
| `bbc.co.uk` / `bbc.com` | 普通海外代理 | 前者流媒体组，后者直连 |
| `sciencedirect.com` | 学术代理，优先于国内规则 | 直连 |
| `otel.cline.bot` | REJECT | REJECT |
| 当前两个自定义代理域名 | 沿用内地现有匹配，不新增专组 | AI 组 |
| 新构造的未命中样本 | 代理兜底 | 直连兜底 |

逐项记录：命中的规则、目标组、当前选项、最终出口和访问结果。YAML 两种节点布局、iOS 各自验证；HTTP 返回、DNS 返回、代理出站与服务账号可用性分开判断。

验收结果使用“通过／失败／未执行”三种状态。语法通过不等于节点可用，测速通过不等于 AI 或流媒体可用，未测项目不得写成通过。

## 11. 明天的执行顺序与交付格式

1. **冻结基线**：记录仓库提交和文件校验值；只读记录重装后的实际客户端版本、初始化选项和 TUN 开启状态，不改变应用。
2. **验证导入即用路径**：核对真实合并机制，只验证初始化设置且 TUN 开启的路径；同时提取六份文件、七份 `.list`、默认出口与特例的行为清单。
3. **成对重写 YAML**：在通过兼容验证的路径上形成同场景共享行为，再编写单地区／多地区分组及必要初始回退；保留全部现有覆盖。
4. **重写两份 CONF**：保留 iOS 原有语义，同步依赖清单和场景差异。
5. **完成离线验证**：结构、引用、空组、正则、合并、内核加载及远程集检查；必要时先适配旧校验脚本。
6. **更新文档并交付**：给出六份主文件、配套清单、只需初始化后开启 TUN 并导入绑定的 README、Z1–Z6 验证报告、行为差异与恢复步骤。
7. **现场启用与发布**：属于独立步骤；后续任务若只要求重写文件，完成可审阅交付后结束，不自动切换正在工作的配置或推送远端。

最终报告应简洁回答：是否真正实现无需额外设置、重写了哪些文件、保留了哪些默认行为、有哪些必要适配、哪些检查已通过、哪些需目标设备验收，以及如何恢复基线。

恢复分两层：仓库恢复基线配置文件；客户端撤回本次新覆写，恢复此次重装后、导入新覆写前的订阅与绑定状态，其他应用选项仍为初始化值且 TUN 开启。禁止恢复重装前的手工 DNS、应用设置或旧运行时配置。不要把只回退 Git 说成已经恢复客户端；恢复后仍需验证实际合成结果。

## 12. 可直接交给 AI 的执行指令

```text
请读取本仓库 REWRITE-SPEC.md，并据此完整重写以下六个文件：
rule_single.yaml、rule_multi.yaml、rule_special.yaml、rule_special_multi.yaml、
shadowrocket.conf、shadowrocket_special.conf。

以当前回滚后的 main 为服务分流基线。
唯一客户端基准是全新安装、初始化状态的 Clash Party，仅开启 TUN。
我已经彻底重装，只开启了 TUN 并下载了订阅；后续只需添加并绑定覆写即可使用。
DNS 覆写、嗅探覆写及其余应用选项必须保持初始化值，不要求我改，也不能代替我偷偷改。
不要恢复重装前的应用设置、DNS 池、旧缓存或确认记录；不要让我重新初始化已经重装好的客户端。
保持服务覆盖、规则优先级、代理／直连意图与有效候选下的家宽／地区偏好。
允许为导入即用作最小必要的合并兼容和初始节点回退，并记录差异、验证结果。
不要恢复回滚前备份分支的试验改动，不要混入第 9 节的可选行为调整。

保留文件名与使用方式，同步需要更新的七份 Shadowrocket 补充清单和 README。
先核对当前工作区，再完整实现、验证并交付；不要只给建议或简化配置。
发现与方案不同的现状时，先保留用户已有改动，记录差异并完成不受影响的工作。

验证必须区分结构、客户端合并、内核加载、远程规则与实际业务，并完成 Z1–Z6 检查。
所有检查都在初始化设置且 TUN 开启的同一基准下完成，不再要求多套 DNS／嗅探开关组合。
导入、更新和重启前后都要核对应用选项保持初始化值，不能将“用户再调整几个开关”作为交付条件。
若纯 YAML 受目标客户端限制无法满足某项要求，指出具体证据并标记未满足，不能虚报导入即用。
只能报告实际完成的测试，缺少 iOS 环境时列出待实机验证项。
输出相对基线的行为差异表和恢复步骤。
本任务默认只改仓库文件，不切换正在运行的客户端，不自动发布。
```
